RewriteEngine On

# Redirect root to main directory
RewriteRule ^$ main/index.php [L,R=301]

# Handle requests for main directory
RewriteRule ^main/(.*)$ main/$1 [L]

# Prevent access to sensitive files
<Files ~ "^\.">
    Order allow,deny
    Deny from all
</Files>